# 🔢 Projeto: Jogo do Número Secreto (Primeiro Front-end)

Este projeto marca o meu primeiro desenvolvimento Front-end utilizando HTML, CSS e JavaScript. É um jogo interativo onde o usuário deve adivinhar um número secreto gerado aleatoriamente, com *feedback* imediato a cada tentativa.

O objetivo principal deste projeto foi aplicar os **fundamentos da lógica de programação** e a **manipulação básica do DOM** (Document Object Model) em um ambiente prático.

## 💡 Conceitos Essenciais Aplicados

Este projeto demonstra a aplicação sólida dos pilares da programação em JavaScript:

* **Variáveis e Tipos de Dados:** Armazenamento do número secreto (`let numeroSecreto`) e contagem de tentativas.
* **Funções (`function`):** Modularização do código para iniciar o jogo, verificar o chute e exibir mensagens (`exibirTextoNaTela()`).
* **Condicionais (`if`/`else`):** Lógica de **Decisão** para comparar o chute do usuário com o número secreto e dar *feedback* ("Maior" ou "Menor").
* **Loops (`while`):** Utilizado para forçar a entrada de dados dentro do intervalo correto (se necessário).
* **Manipulação Básica do DOM:** Alteração dinâmica de elementos HTML (como títulos e parágrafos) para exibir as mensagens ao usuário.
* **`return`:** Uso da palavra-chave para devolver valores e controlar o fluxo das funções (ex: gerar um novo número).

## ⚙️ Tecnologias Utilizadas

* **HTML5:** Estruturação básica do jogo.
* **CSS3:** Estilização simples para melhor experiência do usuário.
* **JavaScript (ES6+):** Lógica principal do jogo e toda a interatividade.

## 🚀 Como Executar o Jogo

1.  **Clone o Repositório:**
    ```bash
    git clone [LINK DO SEU REPOSITÓRIO]
    ```
2.  **Navegue até a Pasta:**
    ```bash
    cd nome-do-repositorio
    ```
3.  **Abra no Navegador:**
    Abra o arquivo `index.html` diretamente no seu navegador de preferência (Chrome, Firefox, etc.).

## 🎓 Status do Projeto

* **Status:** Concluído.
* **Propósito:** Atividade prática para consolidação de fundamentos de JavaScript, conforme curso Alura.

---

**Desenvolvido por:** Kaique Ziantoni Rosa
